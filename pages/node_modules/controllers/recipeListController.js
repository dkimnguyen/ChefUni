'use strict';
angular.module('chefUni')
.controller('recipeListCtrl',  ['$scope', '$rootScope', '$http',
  function($scope,  $rootScope, $http) {
        $scope.getLists = function() {
            $http.get('/data/recipeList.json')
            .success(function(data, status, headers) {
               $scope.recipes = data;
               //var recipes = [];

/*               for (var i = 0; i < data.length; i++) {
                   recipes[i] = {
                                       "name" : data[i].recipeName,
                                        "recipeId": data[i].recipeID,
                                        "chefName" : data[i].chefName,
                                        "ingredients": data[i].ingredients,
                                        "prep" : data[i].prep,
                                        "description": data[i].desc,
                                        "cooktime" : data[i].cooktime,
                                        "servings": data[i].servings,
                                        "image": data[i].image,
                                        "instructions": data[i].instructions,
                                        "sharedWith": data[i].sharedWith
                                      }
}*/
 //              $rootScope.recipes = recipes;
            });
        };
    }
]);
