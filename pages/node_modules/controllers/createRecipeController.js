'use strict';
angular.module('chefUni')
.controller('createRecipeCtrl',  ['$scope', '$rootScope', '$http',
  function($scope,  $rootScope, $http) {
        $scope.createRecipe = function(newRecipe) {
            var recipeKeys = [
                "name",
                "recipeName",
                "ingredients",
                "prep",
                "cooktime",
                "servings",
                "instructions"
            ]
        };
    }
]);